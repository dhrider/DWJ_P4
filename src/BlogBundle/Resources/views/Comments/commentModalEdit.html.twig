<button type="button" class="btn btn-xs" title="Editer" data-toggle="modal" data-target="#commentEditDialog{{ comment.id }}">
    <span class="glyphicon glyphicon-edit"></span>
</button>
<div class="modal fade" id="commentEditDialog{{ comment.id }}" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="myModalLabel">Confirmation de Modification</h4>
            </div>
            <div class="modal-body">
                Voulez vous vraiment annuler le signalement du commentaire de {{ comment.author }} du {{ comment.date | date('d-m-Y') }} ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Non</button>
                <a href="{{ path('blog_adminComment_edit', { 'id': comment.id }) }}" class="btn btn-danger">Oui</a>
            </div>
        </div>
    </div>
</div>